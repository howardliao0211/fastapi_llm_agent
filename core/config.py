import os
from dotenv 

class Settings:
    title: str = "GenAI Inference Server"
    description: str = "Inference Server Powered by GenAI"
    version: str = "1.0.0"

settings = Settings()
